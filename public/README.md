# public/ assets to supply

Drop these files here before launch — they are referenced by the site:

- `resume.pdf`   — the real résumé. All "Résumé ↓" buttons download `/resume.pdf`.
- `portrait.jpg` — optional 4:5 About photo. After adding, set
  `about.portrait` to `/portrait.jpg` in `src/content/site.ts`.
  Until then the About page shows the hatch placeholder.
